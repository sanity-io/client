{
  "name": "@sanity/client-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Isolated TypeDoc install so it can run against a TypeScript that still ships the compiler API",
  "devDependencies": {
    "typedoc": "^0.28.16",
    "typescript": "6.0.3"
  }
}
